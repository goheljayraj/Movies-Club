import React from 'react';

const Spotlight = ({id}) => {
    return (
        <div className="spotlight-container">
            <div className="banner">{id}</div>

        </div>
    );
};

export default Spotlight;